SELECT
    COUNT(*)
FROM
    app_user;

SELECT
    COUNT(*)
FROM
    app_thread;

SELECT
    COUNT(*)
FROM
    app_comment;

SELECT
    COUNT(*)
FROM
    app_group;

SELECT
    COUNT(*)
FROM
    app_group_user;
